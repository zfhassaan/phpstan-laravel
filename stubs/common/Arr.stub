<?php

namespace Illuminate\Support;

class Arr
{
   /**
    * Get a value from the array, and remove it.
    *
    * @template TArray of array
    * @param TArray $array
    * @param string|int $key
    * @param mixed $default
    * @return mixed
    * @param-out TArray $array
    */
   public static function pull(&$array, $key, $default = null);

    /**
     * Pluck an array of values from an array.
     *
     * @template TValue
     * @template TPluckValueReturn = mixed
     * @template TPluckKeyReturn of array-key = array-key
     * @param  iterable<TValue>  $array
     * @param  int|string|array<array-key, string>|(\Closure(TValue): TPluckValueReturn)|null  $value
     * @param  string|array<array-key, string>|(\Closure(TValue): TPluckKeyReturn)|null  $key
     * @return array<TPluckKeyReturn, TPluckValueReturn>
     */
     public function pluck($array, $value, $key = null);

    /**
     * @template TKey of array-key
     * @template TValue
     * @param  array<TKey, TValue>  $array
     * @param  int|null  $number
     * @param  bool  $preserveKeys
     * @return ($number is null ? TValue : ($preserveKeys is true ? array<TKey, TValue> : list<TValue>))
     */
    public static function random($array, $number = null, $preserveKeys = false);

    /**
     * @template TKey of array-key
     * @template TValue
     * @param  array<TKey, TValue>  $array
     * @param  (callable(TValue, TKey): bool)|null  $callback
     * @return TValue
     */
    public static function sole($array, $callback = null);
}
